version: '3.8'

services:
  books-api:
    image: ghcr.io/bensaviofernandez/books-api:${TAG}  # whatever tag you’re pushing
    ports:
      - "5000:5000"
    # optional, lets Docker itself know how to check health  
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:5000/health"]
      interval: 30s
      timeout: 5s
      retries: 3

