version: '3.8'

services:
  books-api:
    image: ${REGISTRY}:${IMAGE_TAG}
    ports:
      - "5000:5000"
    environment:
      FLASK_ENV: production
    command: flask run --host=0.0.0.0 --port=5000

  # (optional) add any other prod services here, e.g. a database, redis, etc.
